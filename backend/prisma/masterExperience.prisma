model MasterExperiences{
    id    String @id @default(uuid())
    user User @relation(fields: [userId], references: [id])
    userId String
    category String
    title String
    description String
    createdAt DateTime @default(now())
}